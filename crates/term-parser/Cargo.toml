[package]
name = "term-parser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ANSI/VT parser adapter and terminal-state application."

[lints]
workspace = true

[dependencies]
term-core = { path = "../term-core" }

[dev-dependencies]
proptest.workspace = true
