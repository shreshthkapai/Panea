[package]
name = "diagnostics"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Doctor commands, capability reports, logs, and performance overlays."

[lints]
workspace = true

[dependencies]
config-core = { path = "../config-core" }
platform-core = { path = "../platform-core" }
render-core = { path = "../render-core" }
semantics = { path = "../semantics" }
