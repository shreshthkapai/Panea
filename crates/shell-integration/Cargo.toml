[package]
name = "shell-integration"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shell hooks, OSC semantic events, and installer helpers."

[lints]
workspace = true

[dependencies]
semantics = { path = "../semantics" }
