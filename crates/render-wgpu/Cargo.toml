[package]
name = "render-wgpu"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "GPU renderer implementation, glyph atlas, batches, and frame scheduler."

[lints]
workspace = true

[dependencies]
bytemuck.workspace = true
font-system = { path = "../font-system" }
render-core = { path = "../render-core" }
wgpu.workspace = true
winit.workspace = true
