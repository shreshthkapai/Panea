[package]
name = "term-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Platform-neutral terminal grid, cursor, modes, scrollback, selection, and resize state."

[lints]
workspace = true

